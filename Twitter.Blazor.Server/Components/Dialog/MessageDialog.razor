@if (ShowDialog)
{
    <div class="modal fade show" id="exampleModal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="titleLabel">New Message</h5>
                    <button type="button" class="close" @onclick="@Close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">

                    <EditForm Model="@PrivateMessage" OnValidSubmit="@HandleValidSubmit">

                        <DataAnnotationsValidator />
                        <ValidationSummary />

                        @if (HasError)
                        {
                            <div class="alert alert-danger" role="alert"> @Messege</div>
                        }
                        <div>
                            <label>@Nameto</label>
                        </div>
                        <div class="form-group">
                            <label for="Message">Message: </label>
                            <InputTextArea id="Message" class="form-control" @bind-Value="@PrivateMessage.Message"></InputTextArea>
                        </div>

                        <button type="submit" class="btn btn-primary">Send</button>
                        <a class="btn btn-outline-primary" @onclick="@Close">Close</a>
                    </EditForm>

                </div>
            </div>
        </div>
    </div>
}